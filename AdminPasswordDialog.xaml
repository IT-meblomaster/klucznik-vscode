<Window x:Class="Klucznik.AdminPasswordDialog"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        xmlns:materialDesign="http://materialdesigninxaml.net/winfx/xaml/themes"
        Title="Hasło administratora"
        Width="520"
        MinHeight="260"
        SizeToContent="Height"
        WindowStartupLocation="CenterOwner"
        ResizeMode="NoResize"
        ShowInTaskbar="False"
        Background="#F5F7FB">

    <Border Margin="20"
            Padding="26"
            Background="White"
            BorderBrush="#DDE5F0"
            BorderThickness="1"
            CornerRadius="14">
        <Grid>
            <Grid.RowDefinitions>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
                <RowDefinition Height="Auto"/>
            </Grid.RowDefinitions>

            <TextBlock Grid.Row="0"
                       Text="Podaj hasło administratora:"
                       FontSize="14"
                       Margin="0,0,0,12"/>

            <PasswordBox x:Name="PasswordBox"
                         Grid.Row="1"
                         Height="46"
                         MinWidth="420"
                         Margin="0,0,0,10"
                         Padding="10,0"
                         VerticalContentAlignment="Center"
                         materialDesign:HintAssist.Hint="Hasło"
                         KeyDown="PasswordBox_KeyDown"/>

            <TextBlock x:Name="ErrorTextBlock"
                       Grid.Row="2"
                       Foreground="#B91C1C"
                       FontSize="13"
                       TextWrapping="Wrap"
                       MinHeight="18"
                       Margin="0,0,0,18"/>

            <StackPanel Grid.Row="3"
                        Orientation="Horizontal"
                        HorizontalAlignment="Right"
                        VerticalAlignment="Center">
                <Button Width="130"
                        Height="42"
                        MinWidth="130"
                        MinHeight="42"
                        Padding="14,0"
                        Margin="0,0,10,0"
                        IsDefault="True"
                        Click="Ok_Click">
                    <TextBlock Text="OK"
                               TextAlignment="Center"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"/>
                </Button>

                <Button Width="130"
                        Height="42"
                        MinWidth="130"
                        MinHeight="42"
                        Padding="14,0"
                        IsCancel="True"
                        Click="Cancel_Click">
                    <TextBlock Text="Anuluj"
                               TextAlignment="Center"
                               VerticalAlignment="Center"
                               HorizontalAlignment="Center"/>
                </Button>
            </StackPanel>
        </Grid>
    </Border>
</Window>

